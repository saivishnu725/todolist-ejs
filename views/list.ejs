<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo List</title>
    <link rel="stylesheet" href="style.css" />
  </head>

  <body>
    <!-- Day -->
    <% if (day === "Sunday" || day === "Saturday") {%>
    <h1><%= day %> List</h1>
    <% } else {%>
    <h1><%= day %> List</h1>
    <% } %>
    <!-- Date -->
    <p>Today is: <%= date %></p>
    <!-- List -->
    <ul>
      <% for(i in items) { %>
      <li><%= items[i] %></li>
      <% } %>
    </ul>
    <!-- Input stuff -->
    <form action="/" method="post">
      <input type="text" name="item" autofocus="true" 1 />
      <button type="submit">Add</button>
    </form>
  </body>
</html>
